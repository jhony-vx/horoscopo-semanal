name: Publish weekly zodiac ranking

on:
  schedule:
    - cron: '0 10 * * 1'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: zodiac-content-publication
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Prepare weekly drafts
        run: node scripts/publish-periodic-content.mjs prepare-week

      - name: Validate drafts
        run: |
          npm run check
          npm run build

      - name: Publish complete weekly group
        run: node scripts/publish-periodic-content.mjs publish-week

      - name: Validate published group
        run: |
          npm run check
          npm run build

      - name: Commit and push publication
        run: |
          git add src/content/weeks src/content/horoscopes
          if git diff --cached --quiet; then
            echo "No hay cambios semanales para publicar."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "content: publica ranking zodiacal semanal"
          git push origin main
